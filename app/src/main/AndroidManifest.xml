<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.telas_v1">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.mensagens.EnviarPostagemChatActivity"></activity>
        <activity android:name=".activitys.postagemcliente.PostagemAtivaActivity" />
        <activity android:name=".fragmentos.fragmentosmenu.contratar.TrabalhadoresContratosActivity" />
        <activity android:name=".activitys.users.ListFotosActivity" />
        <activity android:name=".activitys.users.myperfil.MyPerfilClienteActivity" />
        <activity android:name=".activitys.users.myperfil.MyPerfilTrabalhadorActivity" />
        <activity android:name=".fragmentos.fragmentosmenu.buscar.FiltrarTrabalhadorActivity" />
        <activity android:name=".activitys.users.otherperfil.PerfilTrabalhadorActivity" />
        <activity android:name=".activitys.mensagens.ConversasActivity" />
        <activity android:name=".activitys.postagemcliente.PostagemActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activitys.postagemcliente.MapNewPostActivity"
            android:label="@string/title_activity_map_new_post" />
        <activity android:name=".activitys.postagemcliente.NovaPostagemClienteActivity" />
        <activity android:name=".activitys.mensagens.ChatActivity" />
        <activity android:name=".fragmentos.fragmentosmenu.MenuActivity" />
        <activity android:name=".activitys.startactivitys.RecuperarSenhaActivity" />
        <activity android:name=".fragmentos.fragmentoscriarconta.CreateActivity" />
        <activity android:name=".activitys.startactivitys.LoginActiviy" />
        <activity android:name=".activitys.startactivitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>